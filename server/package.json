{
  "name": "url-shortener-server",
  "version": "1.0.0",
  "description": "Node.js server for URL shortener API",
  "main": "src/index.js",
  "scripts": {
    "build": "cd ../client && npm run build",
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sql.js": "^1.10.2"
  }
}
